[pytest]
# This repo vendors an editable dependency under src/ccs-ai-josh/.
# Its tests pull in optional deps (e.g. Azure OpenAI, reportlab, LangSmith hub)
# and can fail CI for this app. Ignore it during discovery.
norecursedirs =
    src/ccs-ai-josh

